import { circle } from "@wormhole-foundation/sdk-base";
export const circleDomainItem = {
    binary: "uint",
    size: 4,
    custom: {
        to: (id) => circle.toCircleChain(id),
        from: (name) => circle.circleChainId(name),
    },
};
export const circleNonceItem = {
    binary: "uint",
    size: 8,
};
//# sourceMappingURL=circle.js.map