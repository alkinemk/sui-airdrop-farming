import { JsonRpcProvider } from '@mysten/sui.js';
import { ChainId } from '@wormhole-foundation/connect-sdk';
export declare function getForeignAsset(provider: JsonRpcProvider, tokenBridgeStateObjectId: string, originChainId: ChainId, originAddress: Uint8Array): Promise<string | null>;
