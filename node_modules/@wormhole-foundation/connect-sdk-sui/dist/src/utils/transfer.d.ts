import { ChainId } from '@wormhole-foundation/connect-sdk';
import { SuiCoinObject } from './types';
import { JsonRpcProvider, TransactionBlock } from '@mysten/sui.js';
export declare function transferFromSui(provider: JsonRpcProvider, coreBridgeStateObjectId: string, tokenBridgeStateObjectId: string, coins: SuiCoinObject[], coinType: string, amount: bigint, recipientChainId: ChainId, recipient: Uint8Array, feeAmount?: bigint, relayerFee?: bigint, payload?: Uint8Array | null, coreBridgePackageId?: string, tokenBridgePackageId?: string): Promise<TransactionBlock>;
